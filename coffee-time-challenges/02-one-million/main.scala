object OneMillion {
    def main(args: Array[String]) {
        println("%4.0f * %4.0f = %4.0f".format(math.pow(2, 6),
                                               math.pow(5, 6),
                                               math.pow(10, 6)));
    }
}